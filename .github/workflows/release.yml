name: release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - { goarch: amd64, suffix: amd64 }
          - { goarch: arm64, suffix: arm64 }
          - { goarch: arm, goarm: "7", suffix: armv7 }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Build panel
        env:
          CGO_ENABLED: "0"
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
          GOARM: ${{ matrix.goarm }}
        run: |
          go build -trimpath -ldflags "-s -w" -o pg-panel ./cmd/panel
          tar czf "pg-panel_linux_${{ matrix.suffix }}.tar.gz" pg-panel
      - uses: actions/upload-artifact@v4
        with:
          name: pg-panel_linux_${{ matrix.suffix }}
          path: "pg-panel_linux_${{ matrix.suffix }}.tar.gz"

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: "pg-panel_*"
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*.tar.gz
          generate_release_notes: true
